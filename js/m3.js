/**
 * @namespace m3
 */
const m3 = {
  /**
   * Defines reusable interface components.
   * They leverage {@link m3.utility.component.inventFactory} to provide a consistent interface.
   *
   * @namespace m3.component
   */
  component: {},
  /**
   * Defines reusable data structures.
   * They leverage {@link m3.utility.model.inventFactory} to provide a consistent interface.
   *
   * @namespace m3.model
   */
  model: {},
  /**
   * Defines utilities that solve common problems for game controllers.
   *
   * @namespace m3.utility
   */
  utility: {},
}

/**
 * Provides functions that would typically be provided by a framework or game engine.
 *
 * @namespace utility
 */
const utility = {}
