Singleton interface components that make up the prototype.

Please assign these to the `m3.app` namespace:
```js
/**
 * @file js/m3/app/foo.js
 */
'use strict'

m3.app.foo = (
  function fooIIFE(undefined) {

    return {
      
    }
  }
)()
```
